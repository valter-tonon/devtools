import * as React from 'react';
import {Page} from "@/presentation/components/Page/Page";

type Props = {

};
export const Dashboard = (props: Props) => {
    return (
        <Page title={'Dashboard'}>
            Dashboard
        </Page>
    );
};
